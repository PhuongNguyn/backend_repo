export enum HttpStatusCode {
  OK = 200,
  BAD_REQUEST = 400,
  NOT_FOUND = 404,
  UNAUTHORIZED = 403,
  UNAUTHENTICATED = 401,
  INTERNAL_SERVER = 500,
}

export enum Role {
  ADMIN = "admin",
  USER = "user",
}
